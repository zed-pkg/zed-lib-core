# Virtual workspace for the shared polyglot lib-core package.
# Executable ORM crates belong only in zed-pkg/zed-orm-core.
[workspace]
resolver = "3"
members = ["src/rust"]
default-members = ["src/rust"]
